import CurrentWeather from '../components/CurrentWeather.vue';

describe('CurrentWeather Component', () => {
  beforeEach(() => {
    cy.mount(CurrentWeather);
  });

  // it('renders correctly', () => {
  //   cy.get('.current-weather').should('exist');
  // });
});
